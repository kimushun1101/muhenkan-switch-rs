{
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["main", "help"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "store:default",
    "autostart:default"
  ]
}
